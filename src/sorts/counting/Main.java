package sort.counting;import java.util.Arrays;public class Main {    public static void main(String[] args) {        int[] intArray = {12, 12, 6, 9, 10, 12, 12, 12, 11, 8, 9, 2, 3, 4, 1};//        int[] intArray = {2, 5, 9, 8, 2, 8, 7, 10, 4, 3};        countingSource(intArray, 12);//        System.out.println(Arrays.toString(countingArray));        System.out.println("sorted array : "+Arrays.toString(intArray));    }    public static void countingSource(int[] source, int greatestValue) {        int[] countingArray = new int[source.length];        System.out.println("counting array before: " + Arrays.toString(countingArray));        int smallestValue = greatestValue - source.length + 1;        System.out.println("smallest value : " + smallestValue);        for (int value : source) {            countingArray[value - smallestValue] = countingArray[value - smallestValue] + 1;        }        System.out.println("counting array after: " + Arrays.toString(countingArray));        sortSourceArray(source, countingArray, smallestValue);    }    private static void sortSourceArray(int[] source, int[] countingArray, int smallestValue) {        int sourceArrayIndex = 0;        for (int i = 0; i < countingArray.length; i++) {            int countingArrayValue = countingArray[i];            while (countingArrayValue > 0) {                source[sourceArrayIndex] = smallestValue + i;                sourceArrayIndex++;                countingArrayValue--;            }        }    }}